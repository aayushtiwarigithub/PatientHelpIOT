import { environment } from 'src/environments/environment';

export const APIENDPOINTConstants = {
    GETRESULT: `${environment.APP_BASE_HREF}/api`,
    SENDSMS: `${environment.APP_BASE_HREF}/sms`,
}